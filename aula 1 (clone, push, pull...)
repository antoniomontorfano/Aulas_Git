*Criacao repositorio (gitignore, git clone, git push)
    basicamente criei um repositório com README, que tu mete um descricao nele
    pude configurar ele com a opcao do gitignore, que filtra os documentos que vao entrar nesse repositorio
    usei o metodo git clone ('git clone (link do repositorio) (nome que eu quero dar p meu repositorio local)'), que faz a conexao do meu repositorio no github com o meu computador, por meio do meu git
    quando fiz o push pro github, ele reconhece o novo commit, mas nao cria outro arquivo.. so atualiza, obviamente (a menos que eu crie outro arquivo)
*Sobre git log
    quando faco muitos commits, eles passam a ficar mt extensos pra exibir de uma vez quando a gente faz o git log... e o computador nao retorna tudo de uma só vez pra isso a gente vai descendo com a tecla ate o commit que a gente desejar, copia oq quiser, e aperta Q pra sair do log (Q de quit)
*Sobre git commit (git add ., git commit -am 'mensagem')
    caso esteja fazendo alteracoes em um arquivo existente, posso fazer o commit e o rastreamento (add) ao mesmo tempo, por meio do 'git commit -am 'mensagem''
    *Duvida
        # eu nao preciso ficar fazendo commit toda hora... posso fazer as alteracoes e apenas adicionar ao meu git, e ficará salvo no meu computador... qnd eu finalizar meu trabalho, meto o commit

        # o objetivo do commit é mostrar o passo a passo feito.. se uma alteracao foi feita em dois dias, eu nao preciso meter o commit nos dois dias.. apenas trackear ja é suficiente, por meio do git add .

